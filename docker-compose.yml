services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nbeauty
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      # Versioned volume name forces a clean local cluster after credential/schema resets.
      - db_data_v2:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d:ro

  backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "8000:8000"

  bot:
    build:
      context: .
      dockerfile: ./bot/Dockerfile
    command: ["python", "-m", "bot.main"]
    env_file:
      - .env
    depends_on:
      - db

volumes:
  db_data_v2:
